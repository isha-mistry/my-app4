# Environment Variables
# Copy this file to .env and fill in the values

# Deployed ERC20 token address
NEXT_PUBLIC_TOKEN_ADDRESS=

# Private key for deployment and transactions (required) [secret]
PRIVATE_KEY=

# URL of the ERC20 deployment API
ERC20_DEPLOYMENT_API_URL=http://localhost:4000

# Deployed ERC721 NFT address
NEXT_PUBLIC_NFT_ADDRESS=

# Private key for deployment and transactions (required) [secret]
PRIVATE_KEY=

# URL of the ERC721 deployment API
ERC721_DEPLOYMENT_API_URL=http://localhost:4001

# Network for Ostium trading (arbitrum or arbitrum-sepolia) (required)
NEXT_PUBLIC_OSTIUM_NETWORK=arbitrum

# Maxxit API base URL
MAXXIT_API_BASE_URL=http://localhost:5000

# Alchemy API key for fetching onchain activity (required)
ALCHEMY_API_KEY=

# Network for onchain activity (arbitrum or arbitrum-sepolia) (required)
NEXT_PUBLIC_ONCHAIN_NETWORK=arbitrum

# Alchemy API key for fetching onchain activity (required)
ALCHEMY_API_KEY=

# Network for onchain activity (arbitrum or arbitrum-sepolia) (required)
NEXT_PUBLIC_ONCHAIN_NETWORK=arbitrum

# Bot token from @BotFather (required) [secret]
TELEGRAM_BOT_TOKEN=

# AIXBT API Key for market intelligence (required) [secret]
AIXBT_API_KEY=

# Superposition network (mainnet or testnet)
NEXT_PUBLIC_NETWORK=mainnet

# Li.Fi integrator ID for bridge
NEXT_PUBLIC_LIFI_INTEGRATOR=cradle-superposition-bridge

# Dune Analytics API key for blockchain data queries (required) [secret]
DUNE_API_KEY=

# Dune Analytics API key for blockchain data queries (required) [secret]
DUNE_API_KEY=

# Dune Analytics API key for blockchain data queries (required) [secret]
DUNE_API_KEY=

# Dune Analytics API key for blockchain data queries (required) [secret]
DUNE_API_KEY=

# Dune Analytics API key for blockchain data queries (required) [secret]
DUNE_API_KEY=

# Dune Analytics API key for blockchain data queries (required) [secret]
DUNE_API_KEY=

# WalletConnect Cloud project ID for wallet connections (required)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Application name displayed in wallet dialogs
NEXT_PUBLIC_APP_NAME=My DApp